services:
  frontend:
    build:
      context: .
    environment:
      NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:-http://host.docker.internal:8000}
      GATE_FIXTURE_MODE: "0"
      NEXT_PUBLIC_GATE_FIXTURE_MODE: "0"
    ports:
      - "3000:3000"
